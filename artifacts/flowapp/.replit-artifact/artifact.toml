kind = "web"
previewPath = "/"
title = "FlowApp"
version = "1.0.0"
id = "artifacts/flowapp"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20338

[services.development]
run = "pnpm --filter @workspace/flowapp run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/flowapp", "run", "build" ]
publicDir = "artifacts/flowapp/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20338"
BASE_PATH = "/"
